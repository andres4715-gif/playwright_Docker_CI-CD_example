apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: playwright-qa          # App name in argocd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/andres4715-gif/playwright_Docker_CI-CD_example.git'
    targetRevision: HEAD
    path: ./k8s/overlays/qa
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: qa
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true