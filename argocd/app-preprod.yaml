apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: playwright-preprod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/andres4715-gif/playwright_Docker_CI-CD_example.git'
    targetRevision: HEAD
    path: ./k8s/overlays/preprod
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: preprod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true